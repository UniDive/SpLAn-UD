# Subsample UD treebanks to get only spoken data

The code in `build.py` helps in selecting:
* treebanks that only contain spoken data
* subsets of spoken data from treebanks that contain mixed data > criteria for selections are described by functions in `filter.py`